apiVersion: v1
kind: Service
metadata:
  name: invest-train
  namespace: mlops
  labels:
    app: invest-train
spec:
  type: ClusterIP
  selector:
    app: invest-train
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
