# INVEST 모델 학습(Train) API 서버 패키지
# pip install -r requirements.txt
#
# 구성 = 학습 패키지(train_requirements.txt) + 학습 트리거용 API 서버(fastapi/uvicorn)

# API 서버 (학습 트리거)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# 데이터 처리
pandas>=1.5.0
numpy>=1.23.0
awswrangler>=3.0.0       # Athena 조회 (SageMaker/Redshift 미사용)
boto3>=1.26.0

# 머신러닝
scikit-learn>=1.2.0
xgboost>=1.7.0
joblib>=1.2.0

# MLflow (Registry 등록 + 인코더 아티팩트)
mlflow>=2.8.0

# 시각화 (4종 차트 아티팩트)
matplotlib>=3.6.0
seaborn>=0.12.0

# 유틸
python-dotenv>=1.0.0
pytz>=2024.1
